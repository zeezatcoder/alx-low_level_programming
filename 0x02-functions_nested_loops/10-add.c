#include <stdio.h>
#include "main.h"
/**
 * add - This function add two numbers
 * @z: the input
 * @y: the other input
 * Description: to add two nums
 * Return: the result
 */
int add(int z, int y)
{
	return (z + y);
}
